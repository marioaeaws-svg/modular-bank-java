# Paso 4 (see ADR-009): the rabbitmq_prometheus plugin exposes only cluster-wide
# aggregated metrics by default (no "queue" label) — per-queue metrics (needed for
# the consumer-lag dashboard panel: rabbitmq_queue_messages_ready/_unacked per queue)
# must be enabled explicitly.
prometheus.return_per_object_metrics = true
